{% extends "base.html.twig" %}

{% block title %}Mettre a jour son profile{% endblock %}

{% block body %}
    <div class="container">
        <h2 class="text-primary font-weight-light">Mettre a jour son profil</h2>

        {{ form_start(form) }}
        {{ form_widget(form) }}
        <button type="submit" class="btn btn-primary">Mettre a jour</button>
        {{ form_end(form) }}
    </div>
{% endblock %}